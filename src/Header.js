import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header>
      <h1>My Website</h1>
      {/* Add other header elements like navigation menu, logo, etc. */}
    </header>
  );
};

export default Header;
